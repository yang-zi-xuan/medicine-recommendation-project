# ============================================================================
# 中医药知识查询系统 — 依赖清单
# 安装方式：pip install -r requirements.txt
# 推荐 Python 版本：3.10+
# ============================================================================

# ── Web 框架 ──────────────────────────────────────────────────────────────────
streamlit>=1.57.0

# ── 语义检索 ──────────────────────────────────────────────────────────────────
sentence-transformers>=2.2.0
numpy>=1.24.0
pandas>=2.0.0
openpyxl>=3.1.0          # pandas 读取 Symptom_Properties.xlsx 所需

# ── LLM API 调用 ──────────────────────────────────────────────────────────────
requests>=2.28.0

# ── 可视化（可选，缺失时自动降级为纯文本展示）────────────────────────────────
streamlit-echarts>=1.0.0  # 模式 2 Sankey 流向图
streamlit-agraph>=0.0.45  # 模式 3 交互式推理节点图
streamlit-lottie>=0.0.5   # 加载/成功动画

# ── PPT 生成（可选，仅 ppt/ 目录下脚本需要）──────────────────────────────────
python-pptx>=0.6.21
Pillow>=9.0.0
pywin32>=305              # add_simple_animations.py（仅 Windows）
